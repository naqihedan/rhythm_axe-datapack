# 房间页广播：**所有人**（含正在编辑器里的人）都会收到房间页（2026-09-26 用户定）——
#   别人不必自己回总表点【游玩】也能看到名单并加入；每次渲染（开房 / 加入 / 退出）都广播一遍。
#   ★ 因为不清屏（只往聊天栏**追加**行），插到编辑器玩家的聊天栏里也不会破坏他们的编辑器面板。
#   ★ 每人的页面状态互不影响：分发按「值所属号段」（menu/consume），谁点哪个按钮各归各页。
execute as @a run function rhythm_axe:room/render
